{
  "name": "formulaflow-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stateless MCP runtime for FormulaFlow, designed for Cloudflare Workers and ChatGPT custom plugin connections.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "wrangler": "4.124.0"
  },
  "engines": {
    "node": ">=20"
  }
}
